This discussion is audit-facing and intentionally brief.

\paragraph{What was simplified.}
The main manuscript now follows one direct route:
define
\[
\Phi(q):=\Gamma(q)(1-q)\zeta(q),
\]
define balance by $\Phi(q)=0$, state Target~A and Target~B, then derive RH conditionally.
Large architecture language and cross-cutting scaffolding were removed from the core flow.

\paragraph{What remains open.}
The manuscript still has two theorem-level bridges:
zero--balance correspondence on the strip, and critical-line exclusion for balanced strip points.
These are explicit open targets, not established results.

\paragraph{Research posture.}
This remains a proposed proof framework under review.
The argument attempts to show that RH would follow once those two targets are proved.
